// Ques: Reverse Pairs (Leetcode Problem) : Given an integer array nums, return the number of reverse pairs in the array.
// A reverse pair is a pair (i, j) where:
// 0 <= i < j < nums.length and
// nums[i] > 2 * nums[j].

#include <bits/stdc++.h>
using namespace std;

int main()
{
    system("cls");
    
    return 0;
}